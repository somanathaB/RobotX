# RobotX Pi Agent — Dependency Map

Generated by reading every `import`/`from` statement in every `.py` file under `robotx/` and `tests/` (post-refactor tree). No circular imports exist anywhere in this repository.

---

## File-Level Import Graph

```
robotx/config/settings.py
  (no internal deps)
  used by: application/app.py, tests/hardware/*.py (all six)

robotx/hardware/motors.py            (no internal deps)
robotx/hardware/encoders.py          (no internal deps)
robotx/hardware/ultrasonic.py        (no internal deps)
robotx/hardware/ir.py                (no internal deps)
robotx/hardware/camera.py            (no internal deps)
robotx/hardware/gps.py               (no internal deps)
  used by: control/robot_controller.py, application/app.py,
           perception/vision_controller.py (camera.py only),
           tests/hardware/*.py (individually, per script)

robotx/navigation/route_planner.py       (no internal deps)
robotx/navigation/directions_client.py   (no internal deps)
  used by: control/robot_controller.py, application/app.py

robotx/perception/object_tracker.py      (no internal deps)
robotx/perception/object_detector.py     (no internal deps)
robotx/perception/temporal_filter.py     -> object_tracker.py (Track)
robotx/perception/decision_engine.py     (no internal deps)
robotx/perception/vision_controller.py   -> hardware/camera.py, object_detector.py,
                                             temporal_filter.py, object_tracker.py
                                             [EXPERIMENTAL -- exercised only by
                                              tests/hardware/test_vision.py; NOT
                                              imported by control/robot_controller.py
                                              or application/app.py]
  used by: application/app.py (object_detector.py only),
           control/robot_controller.py (object_detector.py only),
           tests/hardware/test_cv.py (object_detector.py only),
           tests/hardware/test_vision.py (vision_controller.py, decision_engine.py,
                                           temporal_filter.py, object_tracker.py)

robotx/control/robot_controller.py
  -> hardware/{motors,encoders,ultrasonic,ir,gps,camera}.py
  -> navigation/{route_planner,directions_client}.py
  -> perception/object_detector.py
  [PRODUCTION -- the only control-loop implementation wired into the running app]
  used by: application/app.py

robotx/communication/socket_client.py
  (no internal deps -- external: python-socketio)
  used by: application/app.py

robotx/application/app.py
  -> communication/socket_client.py
  -> control/robot_controller.py
  -> hardware/{encoders,ir,motors,ultrasonic,gps,camera}.py
  -> navigation/{directions_client,route_planner}.py
  -> perception/object_detector.py
  -> config/settings.py
  [ENTRY POINT -- composition root, imports every package]

tests/hardware/test_motor.py         -> hardware/motors.py, config/settings.py
tests/hardware/test_gps.py           -> hardware/gps.py, config/settings.py
tests/hardware/test_ultrasonic.py    -> hardware/ultrasonic.py, config/settings.py
tests/hardware/test_controller.py    -> hardware/{motors,ultrasonic,ir,encoders}.py, config/settings.py
                                         [does NOT import control/robot_controller.py --
                                          independent reimplementation, see R-05]
tests/hardware/test_cv.py            -> config/settings.py, perception/object_detector.py,
                                         hardware/camera.py
tests/hardware/test_vision.py        -> perception/{vision_controller,decision_engine,
                                         temporal_filter,object_tracker}.py
                                         [the only exerciser of the experimental pipeline]
```

---

## Layering (enforced, verified by grep)

```
config
   ↑
hardware   navigation   perception     (no cross-imports between these three)
   ↑___________↑___________↑
               |
            control
               |
        communication   (parallel to control -- talks to it only via callbacks
                          injected by application/app.py, never imports it directly)
               |
          application    (composition root -- the only file importing everything)
```

**Confirmed absent:**
- No file in `hardware/`, `navigation/`, or `perception/` imports `control/`, `communication/`, or `application/`.
- `control/robot_controller.py` does not import `communication/socket_client.py` or `robotx.application.app` — it is decoupled from the transport via `set_telemetry_hook()` and `handle_command()`, both plain callables wired up by `application/app.py`.
- `communication/socket_client.py` does not import `control/` or `hardware/` — it only knows about dict payloads and a generic `CommandHandler` callable type.
- `perception/vision_controller.py` and `perception/decision_engine.py` do not import `hardware/motors.py`, `control/`, or `communication/` — they cannot command a motor even indirectly; they are pure vision-decision code, currently reachable only from `tests/hardware/test_vision.py`.

---

## Third-Party Dependencies (unchanged by this refactor)

| Package | Used by |
|---|---|
| `fastapi`, `uvicorn` | `application/app.py` |
| `python-socketio[asyncio_client]` | `communication/socket_client.py` |
| `httpx` | `navigation/directions_client.py` |
| `opencv-python` (`cv2`) | `perception/object_detector.py`, `perception/vision_controller.py` |
| `ultralytics` (optional, YOLOv8) | `perception/object_detector.py` |
| `picamera2` | `hardware/camera.py` |
| `pyserial`, `pynmea2` | `hardware/gps.py` |
| `RPi.GPIO` (mock fallback when absent) | `hardware/motors.py`, `hardware/encoders.py`, `hardware/ultrasonic.py`, `hardware/ir.py` |
