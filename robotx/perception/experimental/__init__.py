"""Experimental perception code. NOT part of the production perception path.

Nothing in this package is imported by `robotx.application`, the agent loop, or
`robotx.perception.pipeline`. It is exercised only by
`tests/perception/test_vision.py`, a manual bench script that needs the real
camera. See README.md in this directory.
"""
