fastapi==0.115.6
uvicorn[standard]==0.30.6
python-socketio[asyncio_client]==5.11.4
httpx==0.27.2
opencv-python==4.10.0.84
pyserial==3.5
pynmea2==1.19.0

# Picamera2 (libcamera) is required for Raspberry Pi Camera Module 3.
# Install via apt (recommended on Raspberry Pi OS/Debian):
#   sudo apt update
#   sudo apt install -y python3-picamera2
# NOTE: Picamera2 is typically NOT installed via pip.

# Raspberry Pi GPIO (needed for motor/ultrasonic/encoder/IR tests on real hardware)
RPi.GPIO==0.7.1

# YOLOv8 (vision-only pipeline). This pulls in torch; on Raspberry Pi, use piwheels when available.
# NOTE: `ultralytics` pulls in `torch`, which may not have wheels for your Pi OS/Python version.
# Install YOLO only when you have a compatible torch wheel available:
#   TMPDIR=/home/pi/pip-tmp pip install --no-cache-dir ultralytics
# ultralytics>=8.0.0